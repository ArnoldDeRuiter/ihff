@model IEnumerable<ihff.Models.OrderItemCombined>


@{
    ViewBag.Title = "Index";
    List<ihff.Models.OrderItemCombined> allCombined = ViewBag.AllCombined;
    //ViewBag.allItems = allItems;
    // Html.DisplayFor(model => model.)
}
<h2> Reservations </h2>

@{
    foreach (ihff.Models.OrderItemCombined i in allCombined)
    {
        string typeFolder = "";

        if (i.EventType == 1)
        {
            typeFolder = "films";

        }

            else if (i.EventType == 2)
            {
                typeFolder = "specials";
            }

                else
                {
                    typeFolder = "diners";
                }

        <img src='@string.Format("../Content/img/"+typeFolder+"/{0}", i.Image)' class="item-icon" />

    }

}

<!-- foreach (var s in ordersMetItems)
{
    <p> //s.Ammount </p>
} -->